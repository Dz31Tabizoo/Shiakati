<UserControl x:Class="Shiakati.Views.ClientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             FontFamily="{StaticResource BodyFont}"
             Background="{StaticResource BgBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                     materialDesign:HintAssist.Hint="Rechercher par nom ou téléphone…"
                     Style="{StaticResource ShiakatiTextBox}"
                     Width="300" FontSize="14"/>
            <Button Content="Ajouter un client"
                    Command="{Binding AddNewClientCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Margin="10,0,0,0"
                    FontSize="14" MinHeight="48"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Clients}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectedItem="{Binding SelectedClient}"
                  Style="{StaticResource ShiakatiDataGrid}"
                  ColumnHeaderStyle="{StaticResource ShiakatiDataGridColumnHeader}"
                  CellStyle="{StaticResource ShiakatiDataGridCell}"
                  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding FullName}" MaxWidth="350" Width="*" FontSize="14"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding PhoneNumber}" Width="Auto" FontSize="14"/>
                <DataGridTextColumn Header="Crédits" Binding="{Binding TotalCredits, StringFormat={}{0:N2} DA}" Width="Auto" FontSize="14"/>
                <DataGridTextColumn Header="Versements" Binding="{Binding TotalVersements, StringFormat={}{0:N2} DA}" Width="Auto" FontSize="14"/>
                <DataGridTextColumn Header="Solde" Binding="{Binding NetBalance, StringFormat={}{0:N2} DA}" Width="Auto" FontSize="14" FontWeight="SemiBold"
                                    Foreground="{StaticResource SecondaryBrush}"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource ShiakatiDataGridRow}">
                    <EventSetter Event="MouseDoubleClick" Handler="Row_DoubleClick"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</UserControl>