<UserControl x:Class="Shiakati.Views.ClientDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shiakati.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024"
             FontFamily="{StaticResource MontserratFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Client info -->
        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Client.FullName}" FontSize="24" FontWeight="Bold"/>
                <Button Content="Modifier" Command="{Binding EditClientCommand}" 
                        Style="{StaticResource MaterialDesignFlatButton}" Margin="10,0,0,0"/>
            </StackPanel>
            <TextBlock Text="{Binding Client.PhoneNumber}" FontSize="16" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tabs for Credits and Versements -->
        <TabControl Grid.Row="1" Margin="0,10,0,0">
            <TabItem Header="Crédits">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Add credit form -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Text="{Binding CreditAmount}" materialDesign:HintAssist.Hint="Montant" Width="120"/>
                        <TextBox Text="{Binding CreditNotes}" materialDesign:HintAssist.Hint="Notes" Width="200" Margin="10,0"/>
                        <Button Content="Ajouter" Command="{Binding GrantCreditCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    </StackPanel>

                    <!-- Credits DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Credits}" AutoGenerateColumns="False"
                              MinHeight="150" MaxHeight="300"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              BorderThickness="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Montant" Binding="{Binding Amount, StringFormat={}{0:N2} DA}" Width="Auto"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding GrantedAt, StringFormat=dd/MM/yyyy}" Width="Auto"/>
                            <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                            <DataGridTextColumn Header="Utilisé" Binding="{Binding IsRedeemed}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Credits total -->
                    <TextBlock Grid.Row="2" Margin="0,8,0,0" FontWeight="SemiBold" FontSize="14">
                        <Run Text="Crédits disponibles : "/>
                        <Run Text="{Binding TotalCreditsAvailable, StringFormat={}{0:N2} DA}" Foreground="#2E7D32"/>
                    </TextBlock>
                </Grid>
            </TabItem>

            <TabItem Header="Versements">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Add versement form -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox Text="{Binding VersementAmount}" materialDesign:HintAssist.Hint="Montant" Width="120"/>
                        <TextBox Text="{Binding VersementNotes}" materialDesign:HintAssist.Hint="Notes" Width="200" Margin="10,0"/>
                        <Button Content="Ajouter" Command="{Binding AddVersementCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    </StackPanel>

                    <!-- Versements DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Versements}" AutoGenerateColumns="False"
                              MinHeight="150" MaxHeight="300"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              BorderThickness="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Montant" Binding="{Binding Amount, StringFormat={}{0:N2} DA}" Width="Auto"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding PaymentDate, StringFormat=dd/MM/yyyy}" Width="Auto"/>
                            <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Versements total -->
                    <TextBlock Grid.Row="2" Margin="0,8,0,0" FontWeight="SemiBold" FontSize="14">
                        <Run Text="Total versements : "/>
                        <Run Text="{Binding TotalPayments, StringFormat={}{0:N2} DA}" Foreground="#1565C0"/>
                    </TextBlock>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>