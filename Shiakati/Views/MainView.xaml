<Window x:Class="Shiakati.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Shiakati POS" Height="800" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}" CornerRadius="0,0,0,0">
            <StackPanel Margin="0,20">
                <materialDesign:PackIcon Kind="AlphaSBox" Width="60" Height="60" 
                                         HorizontalAlignment="Center" Foreground="White" Margin="0,0,0,40"/>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Height="100" 
                        Command="{Binding NavigateToDashboardCommand}">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="ViewDashboard" Width="60" Height="60" HorizontalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="Accueil" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Height="100" Margin="0,10"
                        Command="{Binding NavigateToPOSCommand}">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="CashRegister" Width="60" Height="60" HorizontalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="Caisse" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatButton}" Height="100"
                        Command="{Binding NavigateToStockCommand}">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="StoreSettings" Width="60" Height="60" HorizontalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="Stock" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" Effect="{StaticResource MaterialDesignShadowDepth1}">
                <Grid Margin="20,0">
                    <TextBlock Text="SHIAKATI LUXE" VerticalAlignment="Center" 
                               FontFamily="{StaticResource CairoFont}" FontWeight="Bold" FontSize="18" Foreground="{StaticResource PrimaryBrush}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Vendeur: Rafik" VerticalAlignment="Center" Margin="0,0,20,0" FontWeight="SemiBold"/>
                        <materialDesign:PackIcon Kind="AccountCircle" Width="30" Height="30" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="10"/>
        </Grid>
    </Grid>
</Window>