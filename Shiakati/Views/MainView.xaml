<Window x:Class="Shiakati.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Shiakati POS" Height="800" Width="1280"
        Background="{StaticResource BgBrush}"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
    

    <Window.Resources>
        
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- Animations -->
        <Storyboard x:Key="ExpandSidebar">
            <DoubleAnimation Storyboard.TargetProperty="Width" To="250" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="CollapseSidebar">
            <DoubleAnimation Storyboard.TargetProperty="Width" To="120" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NavToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                      VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border x:Name="SideMenu" 
                Grid.Column="0" 
                Background="{StaticResource PrimaryBrush}" 
                CornerRadius="0,20,20,0"
                Panel.ZIndex="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Width" Value="65"/>
                    <!-- Default Width -->
                    <Style.Triggers>
                        <!-- FIX: Use MultiDataTrigger when checking Bindings -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <!--<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>-->
                                <Condition Binding="{Binding ElementName=MenuToggleButton, Path=IsChecked}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource ExpandSidebar}"/>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource CollapseSidebar}"/>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <!-- Toggle Button Logic -->
                        <DataTrigger Binding="{Binding ElementName=MenuToggleButton, Path=IsChecked}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource ExpandSidebar}"/>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource CollapseSidebar}"/>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,20,0,30">
                    <ToggleButton x:Name="MenuToggleButton" 
                                  Style="{StaticResource MaterialDesignActionToggleButton}"
                                  Background="Transparent" BorderBrush="Transparent" Foreground="{StaticResource SecondaryBrush}"
                                  HorizontalAlignment="Right" Margin="0,0,15,10">
                        <ToggleButton.Content>
                            <materialDesign:PackIcon Kind="ArrowRight" Width="25" Height="25"/>
                        </ToggleButton.Content>
                        <materialDesign:ToggleButtonAssist.OnContent>
                            <materialDesign:PackIcon Kind="ArrowLeft" Width="25" Height="25"/>
                        </materialDesign:ToggleButtonAssist.OnContent>
                    </ToggleButton>
                    <materialDesign:PackIcon Kind="DiamondStone" Foreground="{StaticResource SecondaryBrush}" 
                                            Width="48" Height="48" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                </StackPanel>                

                <!--<StackPanel Grid.Row="1">
                    <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToPOSCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CashRegister" Width="48" Height="48" Margin="10,0,0,0"/>
                            <TextBlock Text="Caisse" Margin="35,0,0,0" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource MaterialDesignLightSeparator}" Margin="0,15,0,15"/>
                    <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToStockCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Warehouse" Width="48" Height="48" Margin="10,0,0,0"/>
                            <TextBlock Text="Stock" Margin="35,0,0,0" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </Button>      
                </StackPanel>-->

                <StackPanel Grid.Row="1">
                    <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToPOSCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CashRegister" Width="48" Height="48" Margin="10,0,0,0"/>
                            <TextBlock Text="Caisse" Margin="35,0,0,0" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </Button>

                    <Separator Style="{StaticResource MaterialDesignLightSeparator}" Margin="0,15,0,15"/>

                    <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateToStockCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Warehouse" Width="48" Height="48" Margin="10,0,0,0"/>
                            <TextBlock Text="Stock" Margin="35,0,0,0" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </Button>

                    <Separator Style="{StaticResource MaterialDesignLightSeparator}" Margin="0,15,0,15"/>

                    <ToggleButton x:Name="btnHistorique" Style="{StaticResource NavToggleButtonStyle}">
                        <StackPanel Orientation="Horizontal" Width="230" HorizontalAlignment="Left">
                            <materialDesign:PackIcon Kind="History" Width="48" Height="48" Margin="25,0,0,0"/>
                            <TextBlock Text="Historique" Margin="35,0,0,0" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </ToggleButton>
                    
                    <StackPanel Visibility="{Binding IsChecked, ElementName=btnHistorique, Converter={StaticResource BoolToVis}}">
                        <Button Style="{StaticResource NavButtonStyle}" Margin="30,2,10,2" Height="40">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ReceiptTextClock" Width="30" Height="30" Margin="10,0,0,0"/>
                                <TextBlock Text="Historique Ventes" Margin="25,0,0,0" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Margin="30,2,10,2" Height="40">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="DatabaseClock" Width="30" Height="30" Margin="10,0,0,0"/>
                                <TextBlock Text="Mouvement Stock" Margin="25,0,0,0" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Row="2" Style="{StaticResource NavButtonStyle}" Margin="10,0,10,20" Foreground="{StaticResource SecondaryBrush}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Logout" Width="48" Height="48" Margin="13,0,0,0"/>
                        <TextBlock Text="Déconnexion" Margin="35,0,0,0" FontSize="15" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Control Bar (Draggable Area) -->
            <Border x:Name="pnlControlBar" Grid.Row="0" Background="White" 
                    MouseLeftButtonDown="pnlControlBar_MouseLeftButtonDown"
                    MouseEnter="pnlControlBar_MouseEnter">
                <Grid Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="SHIAKATI LUXE" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnMinmize" Content="—" Click="BtnMinmize_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Gray"/>
                        <Button x:Name="btnMaximize" Content="☐" Click="btnMaximize_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Gray"/>
                        <Button x:Name="btnClose" Content="✕" Click="btnClose_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red"/>
                    </StackPanel>
                </Grid>
            </Border>

            <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="15"/>
        </Grid>
    </Grid>
</Window>