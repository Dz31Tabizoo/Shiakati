<UserControl x:Class="Shiakati.Views.POSContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:Shiakati.Views"
             FontFamily="{StaticResource BodyFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border  Grid.Row="0" Background="{StaticResource BgBrush}"
                BorderBrush="{StaticResource BgAltBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <!-- Larger, touch‑optimised “Nouvelle vente” button -->
                <Button HorizontalAlignment="Right"
                        VerticalAlignment= "Top"
                        Command="{Binding AddNewTabCommand}"
                        Style="{StaticResource PrimaryButton}"
                        MinHeight="35"
                        MinWidth="120"
                        FontSize="18"
                        Margin="0,-6,8,0"
                        
                        >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus"
                                         Width="24" Height="24"
                                         VerticalAlignment="Center"
                                         Margin="0,0,8,0"/>
                        <TextBlock FontWeight="SemiBold" Foreground="{StaticResource BgBrush}" Text="Nouvelle vente" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
        <!-- Tab control -->
        <TabControl Grid.Row="1"
                    ItemsSource="{Binding PosTabs}"
                    SelectedItem="{Binding SelectedTab}"
                    Style="{StaticResource MaterialDesignTabControl}"
                    Background="Transparent">

            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,0">
                        <materialDesign:PackIcon Kind="Receipt"
                                                 Width="16" Height="16"
                                                 VerticalAlignment="Center"
                                                 Foreground="{StaticResource PrimaryBrush}"
                                                 Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding TabName}"
                                   VerticalAlignment="Center"
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Margin="0,0,8,0"/>
                        <Button Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource MaterialDesignIconForegroundButton}"
                                Width="20" Height="20" Margin="0,-2,0,0"
                                ToolTip="Fermer la vente">
                            <materialDesign:PackIcon Kind="Close" Width="12" Height="12"/>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <views:PosView />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</UserControl>